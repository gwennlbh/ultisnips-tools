on:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
      - uses: pnpm/setup-pnpm@v4
      - run: pnpm i
      - run: pnpm build
      
